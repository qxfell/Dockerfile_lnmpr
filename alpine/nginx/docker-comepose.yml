  version: '3'
  services:
    nginx:
      image: alpine/nginx:v1
      ports:
        - "81:80"
      volumes:
        - "/alpine/nginx/html:/usr/share/nginx/html"
        - "/alpine/nginx/nginx.conf:/etc/nginx/nginx.conf"
        - "/alpine/nginx/vhost:/etc/nginx/conf.d"
